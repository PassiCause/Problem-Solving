#include <bits/stdc++.h>
using namespace std;

int n, p, a[200000], b[200000];

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    cin >> n >> p;

    for (int i = 0; i < n; i++) {
        cin >> a[i];
    }

    for (int i = 0; i < n; i++) {
        cin >> b[i];
    }

    
}